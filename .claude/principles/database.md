# 데이터베이스 규칙 (Iron Rule)

> 적용 범위: 백엔드(영속 계층 보유) 프로젝트

- **브라운필드 시스템에서 안전한 배포를 위해 추가적(additive) 스키마 변경을 선호한다.**
  - 컬럼 추가는 `NULL` 허용 + 기본값(default) 전략. NOT NULL 컬럼을 기존 테이블에 즉시 추가하지 않는다.
  - 컬럼 삭제/이름 변경은 두 단계로 나눈다: (1) 새 컬럼을 먼저 추가하고 코드에서 양쪽 호환, (2) 이후 릴리스에서 기존 컬럼 제거.
  - 타입 변경은 새 컬럼 + 데이터 마이그레이션 + 구 컬럼 제거 순서로 분리한다.
- **되돌릴 수 있는 전략의 마이그레이션을 사용한다.**
  - 프로젝트 스키마 관리 방식은 `CLAUDE.md` 에 명시한다.
  - 파괴적 변경은 영향 범위와 **롤백 SQL** 을 PR 본문에 첨부한다.
- **스키마 마이그레이션과 대규모 기능 리팩터링을 하나의 작업에 섞지 않는다.** 커밋 레벨에서도 분리한다.
- **쿼리 로직을 중앙화한다.** 같은 조건을 여러 곳에 중복 작성하지 않는다. DB 접근은 영속 계층(Repository 등)에서 일괄 관리하고, 공통 필터·정렬 조건은 헬퍼/빌더 내부에서 관리한다.
- **트랜잭션으로 쓰기를 묶는다.** 모든 `INSERT` / `UPDATE` / `DELETE` 는 트랜잭션으로 묶고, 실패 시 롤백한다. 여러 테이블에 걸친 변경은 단일 트랜잭션 또는 보상 로직으로 처리한다.
- **SQL Injection 을 방지한다.** ORM 또는 파라미터 바인딩만 사용하고, 사용자 입력을 쿼리 문자열에 직접 concat/format 하는 raw SQL 을 금지한다.
- **핵심 영속성 경로와 에러 케이스에 대한 테스트를 추가한다.** 저장·조회·제약조건 위반을 검증한다.

---

## SQL / 디버그 로깅

- SQL 로깅·쿼리 덤프는 `local` / `development` 환경에서만 활성화한다. 운영에서는 비활성화한다.
- 성능 프로파일링을 위해 운영에 잠깐 켜야 한다면, 작업 종료 후 반드시 복구하고 커밋 로그에 사유를 남긴다.

---

## 외부 인덱스·검색 엔진 사용 시

- 인덱스/컬렉션 구조 변경도 추가적 원칙을 따른다: 신규 인덱스 생성 → 데이터 이전 → 전환. 운영 환경에서 기존 인덱스 삭제 후 재생성 방식은 금지한다.
- 인덱스/컬렉션 이름은 하드코딩하지 않고 환경변수로 주입한다.
- 모든 저장소에서 동일한 문서 식별자를 공유해 데이터 일관성을 보장한다.

---
> 스택별 구체 예시(경로·API·명령)는 각 프로젝트의 `.claude/principles/database.md` 을 참조한다. 이 문서는 스택 무관 공통 베이스다.
